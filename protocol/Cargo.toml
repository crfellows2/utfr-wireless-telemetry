[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = { version = "0.9", features = ["serde"] }
postcard = "1.1.3"
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }

[dev-dependencies]
serde_json = "1.0.149"
